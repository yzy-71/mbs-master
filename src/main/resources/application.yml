server:
  port: 8080
spring:
  application:
    name: BMS
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://47.109.206.82/mbs_master_4group?&useSSL=false&serverTimezone=UTC&characterEncoding=utf8&characterSetResults=utf8
      username: root
      password: 20010720Lmx
mybatis-plus:
  configuration:
    #在映射实体或者属性时，将数据库中表名和字段名中的下划线去掉，按照驼峰命名法映射
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: none
